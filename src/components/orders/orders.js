import React from 'react';
import PropTypes from 'prop-types';
import './orders.scss';

const orders = () => (
  <div className="orders">
    orders Component
  </div>
);

orders.propTypes = {};

orders.defaultProps = {};

export default orders;
